# === Core ===
# MongoDB connection URI
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>/<database>?retryWrites=true&w=majority

# === Authentication ===
# Secret key for signing JWT tokens (use a long, random string)
JWT_SECRET=<your_jwt_secret>
# Access token expiration time (e.g., 15m, 1h, 1d)
JWT_EXPIRES=15m

# === CORS / Frontend ===
# Public URL of the deployed frontend (used for CORS and cookie configuration)
FRONTEND_ORIGIN=https://your-frontend-domain.com

# === Cookies ===
# SameSite policy for authentication cookies.
# Use "None" for cross-domain setups (requires HTTPS in production).
COOKIE_SAMESITE=None

# === Runtime ===
# Node environment mode (development | production)
NODE_ENV=production
# The PORT variable is provided automatically by most hosting services (e.g., Render, Railway, Heroku).
# Do not define manually unless running locally.
# PORT=4000
